ievar mongodb = require('./db');

function User(user){
    this.name = user.name;
    this.password = user.password;
    this.email = user.email;
};

module.exports = User;

User.prototype.save = function(callback){
    var user = {
        name: this.name,
        password: this.password,
        email: this.email
    };

    mongodb.open(function(err, db){
        if(err){
            return callback(err);
        }
        db.conllection('users',function(err, conllection){
            if(err){
                mongodb.close();
                return callback(err);
            }

            conllection.insert(user,{
                safe: true
            }, function(err, user){
                mongodb.close();
                if (err) {
                    return callback(err);
                }
                callback(null,user[0]); //成功！err 为 null，并返回存储后的用户文档
            })
        });
    });
};

User.get = function(db, callback){
    mongodb.open(function(err,db){
        if (err) {
            return callback(err);
        }
        db.conllection('users',function(err,conllection){
            if(err){
                mongodb.close();
                return callback(err);
            }
            conllection.findOne({
                name: name
            },function(err, user){
                mongodb.close();
                if (err) {
                    return callback(err);
                }
                callback(null,user); //成功！返回查询的用户信息
            });
        });
    });
}
