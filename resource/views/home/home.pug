section.article-container
    .container-title
        if user
            span #{user.name}の
        span 小屋
    .article-list
        if list.length > 0
            each item in list
                a.article-list-item(href="/article/" + item.id)
                    .item-author
                        small.item-date= item.update_time
                        span= item.author
                    .content.item-title= item.title
                    .item-desc= item.description
        else
            .txt-center 还木有文章哟 ~~~
